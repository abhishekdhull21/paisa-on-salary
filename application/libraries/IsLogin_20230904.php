<?php            // echo "Er. Vinay Kumar checking is user login or not.";    if (!defined('BASEPATH'))        exit('No direct script access allowed');        class IsLogin {            public function index() {            $ci = & get_instance();            if (empty($_SESSION['isUserSession']['user_id'])) {                $ci->session->set_flashdata('err', "Session Expired, Try once more.");                return redirect(base_url("logout"));            } else if (!empty($_SESSION['isUserSession']['current_login_time'])) {                                                // $ci->load->model('Admin_Model');                    // $query = $ci->Admin_Model->getUser($_SESSION['isUserSession']['user_id']);                    // $user = $query->row_array();                // if (!empty($user['user_last_login_datetime']) && $_SESSION['isUserSession']['current_login_time'] <> $user['user_last_login_datetime']) {                //     $ci->session->set_flashdata('err', "Another login has been made using your credentials. Please contact tech.team@loanwalle.com if you think this is unauthorized.");                //     return redirect(base_url("logout"));                // }            }        }        }